{
  "name": "Penciltest",
  "version": "0.3.1",
  "repository": "git@github.com:monking/penciltest.git",
  "scripts": {
    "start": "tsc && node node_modules/grunt-cli/bin/grunt",
    "debug": "clear; >&2 echo -n \"ENTER to run and paginate \\`tsc --pretty --noEmit\\`. Ctrl+D to end. > \"; i=0; while [[ $i == 0 ]] || read -s; do let i++; tsc --pretty --noEmit | less -R; done; >&2 echo DONE;"
  },
  "devDependencies": {
    "grunt": "^1.6.3",
    "grunt-contrib-concat": "^2.1.0",
    "grunt-sass": "^4.1.0",
    "matchdep": "^2.0.0",
    "sass": "^1.102.0",
    "typescript": "^7.0.2"
  }
}
